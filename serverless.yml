# serverless config
mydocsify:
  component: "@serverless/tencent-website"
  inputs:
    code:
      src: ./mydocsify # Upload static files generated by docsify
      index: index.html
      error: index.html
    region: ap-beijing
    bucketName: my-bucket